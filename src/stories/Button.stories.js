import React from 'react';
import Button from '../components/Button/button';

export default {
  title: 'Button',
  component: Button,
};

export const DefaultButton = () => <Button btnType="primary">Default</Button>;
